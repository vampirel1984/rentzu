# Spec Summary (Lite)

Consolidate the already-applied dark cityscape theme into one shared design-token/style module and run a consistency + polish pass across all mobile screens. Today the dark palette is duplicated and hardcoded in every screen; centralizing colors, spacing, radii, and typography removes drift, speeds future UI work, and perfects visual consistency ahead of production. No backend, API, or new-feature work is included.
