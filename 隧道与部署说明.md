# Rentzu 后端连接方案说明（隧道 / Tunnel）

> 本文档解释：为什么手机 App 连不上你本机的后端、Cloudflare 隧道是怎么解决这个问题的、
> 现在的状态是什么、以及将来要迁移到云服务器时该怎么做。

---

## 一、先说结论

| 问题 | 答案 |
|---|---|
| 我的 FastAPI 还能跑在本机吗？ | **可以，完全不用动** |
| 数据库还在本机吗？ | **是的，PostgreSQL 完全不用动** |
| 语音转文字（Whisper）还在本机吗？ | **是的，录音不会离开你的电脑** |
| 隧道要花钱吗？ | **现在这个方案完全免费** |
| 那有什么代价？ | **你的电脑必须一直开着，而且网址会变** |
| 将来能换成 Railway / Render 吗？ | **能，改一个网址就行** |

**核心概念：隧道不是把你的代码搬走，只是给你本机的服务开了一个"公网入口"。**

---

## 二、为什么手机 App 不能直接连你的 IP？

你之前的想法是：「App 直接访问 `http://我的IP:8000` 不就行了？」

这个想法很自然，但实际上有 **4 个障碍**，每一个都能单独让它失败。

### 障碍 1：内网 IP 只在你家里有效

你这台电脑的 IP 是：

```
192.168.1.105     ← 内网地址（局域网 IP）
```

凡是 `192.168.x.x`、`10.x.x.x`、`172.16-31.x.x` 开头的都是**内网地址**。
这类地址在全世界每个家庭、每个公司里都在重复使用 —— 你家的路由器下有
`192.168.1.105`，你邻居家也有，隔壁公司也有。

所以当测试员的手机用 4G/5G 流量访问 `192.168.1.105` 时，它只会在**自己所在的网络**里找，
永远找不到你的电脑。

> 之所以 Android 模拟器能用 `10.0.2.2`，是因为模拟器就跑在你这台电脑上，
> `10.0.2.2` 是模拟器内部对「宿主机」的特殊别名。真机上这个地址毫无意义。

### 障碍 2：公网 IP 会变，而且被路由器挡住

你的公网 IP 是：

```
98.10.116.84      ← 家庭宽带公网地址
```

问题有三个：

1. **它是动态的** —— 家庭宽带的 IP 会被运营商定期更换（重启光猫、续租都可能变）。
   一旦变了，App 里写死的地址就失效了。
2. **被路由器 NAT 挡住** —— 外部流量到达你的路由器后，路由器不知道该转发给内网哪台设备，
   默认直接丢弃。要让它转发，必须去路由器里做**端口转发（Port Forwarding）**。
3. **安全风险很大** —— 一旦开了端口转发，全世界都能扫描到你家的 8000 端口，
   等于把家里的电脑和数据库直接暴露在公网上。

### 障碍 3：Android 默认禁止 HTTP 明文传输

从 Android 9（API 28）开始，系统**默认禁止**App 发起明文 HTTP 请求，只允许 HTTPS。

```
http://98.10.116.84:8000    ❌ 被 Android 系统直接拦截
https://xxx.com             ✅ 允许
```

而 HTTPS 需要 **SSL 证书**，证书只能签发给**域名**，不能签发给一个裸 IP 地址。
所以就算前两个问题都解决了，这一条也过不去。

### 障碍 4：地址会被编译进安装包

这一点很关键，也是最容易踩坑的地方。

React Native / Expo 打包时，`EXPO_PUBLIC_API_BASE_URL` 会被**直接写死进 JS 包里**，
存放在 AAB 内的 `base/assets/index.android.bundle`。

也就是说：**打包那一刻用的是什么地址，装到手机上就永远是什么地址**，
除非重新打包、重新上传 Google Play。

---

## 三、隧道（Tunnel）是怎么解决的？

### 3.1 传统方式 vs 隧道方式

**传统方式（端口转发）—— 由外向内，问题很多：**

```
测试员手机  ──►  你家路由器（要手动开端口）──►  你的电脑:8000
                      ▲
                      │
              需要固定公网 IP
              需要端口转发
              需要 HTTPS 证书
              全世界都能扫到你家
```

**隧道方式 —— 由内向外，全部绕开：**

```
                    ┌────────────────────┐
测试员手机 ──HTTPS──►│  Cloudflare 服务器  │
                    └─────────┬──────────┘
                              │  ▲
                              │  │ ① cloudflared 主动向外建立的长连接
                              ▼  │
                    ┌────────────────────┐
                    │   你的电脑          │
                    │   cloudflared      │
                    │      ↓             │
                    │   FastAPI :8000    │
                    │      ↓             │
                    │   PostgreSQL       │
                    │   Whisper 模型      │
                    └────────────────────┘
```

### 3.2 关键点：连接方向反过来了

隧道能成立，靠的是**连接方向**这一个巧妙设计。

`cloudflared` 这个小程序运行在你电脑上，它**主动向外**连接 Cloudflare
（就像你用浏览器打开一个网站一样，是「由内向外」）。

路由器和防火墙的默认规则是：
- **由内向外**的连接 → 全部放行 ✅（不然你连网页都打不开）
- **由外向内**的连接 → 全部拦截 ❌

因为 `cloudflared` 是「由内向外」，所以它**天然不受 NAT 和防火墙限制**，
根本不需要动路由器任何设置。

这条长连接建立后就一直保持着。之后测试员的请求先到 Cloudflare，
Cloudflare 再**顺着这条已经存在的通道**把请求送回你的电脑。

### 3.3 因此四个障碍全部解决

| 原障碍 | 隧道如何解决 |
|---|---|
| 内网 IP 外部访问不到 | 测试员访问的是 Cloudflare 的公网域名，不是你的 IP |
| 公网 IP 会变 / 要端口转发 | 完全不需要公网 IP，不需要动路由器 |
| Android 禁止 HTTP | Cloudflare 提供**免费的 HTTPS 证书**，自动生效 |
| 家里电脑暴露在公网 | 你的真实 IP 完全隐藏，外部只看到 Cloudflare |

### 3.4 你的代码一行都不用改

这是最重要的一点：

```
FastAPI       →  照常跑在 127.0.0.1:8000，不用改
PostgreSQL    →  照常跑在本机，不用改
Whisper 模型  →  照常在本机转录，录音不会离开你的电脑
```

隧道只是在**最外面加了一层入口**，内部什么都没变。

> 关于隐私：你之前说不希望录音被传到 OpenAI。用隧道方案时，
> 录音只会从手机传到**你自己的电脑**上转录，是所有方案里隐私最好的。
> （注意：如果将来改成 Railway/Render 托管，录音就会传到那些公司的服务器上。）

---

## 四、免费吗？—— 分两种隧道

`cloudflared` 这个软件本身**永久免费**，隧道功能也免费。区别只在**网址**：

### 4.1 快速隧道（Quick Tunnel）—— 现在用的就是这个

```powershell
cloudflared tunnel --url http://127.0.0.1:8000
```

- ✅ **完全免费**，不用注册账号，不用域名
- ✅ 30 秒就能跑起来
- ❌ **网址是随机的，而且每次重启都会换一个新的**

你现在这个网址是：

```
https://presents-estimate-teeth-stamps.trycloudflare.com
```

**⚠️ 严重注意：这个网址只在 `cloudflared` 这个进程活着的时候有效。**

以下任何一种情况发生，网址就**永久消失，无法找回**：

- 电脑关机 / 睡眠 / 休眠
- Windows 自动更新半夜重启
- 网络断开、光猫重启
- `cloudflared` 进程崩溃

而网址已经被编译进了 AAB。所以一旦网址失效：
**所有测试员的 App 会同时连不上，必须重新打包 + 重新上传 Google Play。**

### 4.2 命名隧道（Named Tunnel）—— 推荐的正式方案

- ✅ 隧道功能**依然免费**
- ✅ 网址固定，重启电脑也不会变（例如 `https://api.rentzu.com`）
- ❌ 需要一个**域名**，大约 **10 美元/年**（≈70 元人民币/年）

**所以「要花钱的」不是隧道，是域名。** 域名的作用就是给你一个永久不变的名字。

> 另外：Google Play 上架时**强制要求**提供一个隐私政策网址（Privacy Policy URL），
> 这个也需要域名或者网页托管，所以域名早晚都要买。

---

## 五、现在的状态

| 项目 | 状态 |
|---|---|
| 后端 FastAPI | ✅ 本机运行中 `0.0.0.0:8000` |
| PostgreSQL | ✅ 本机运行中 |
| Whisper 转录 | ✅ 本机运行（`tiny` 模型，CPU） |
| Cloudflare 隧道 | ✅ 运行中 |
| 当前公网地址 | `https://presents-estimate-teeth-stamps.trycloudflare.com` |
| 已打包的 AAB | `D:\apps\rentzu\build-output\rentzu-v1.0.0-vc3.aab`（33 MB） |
| 版本 | 1.0.0，versionCode 3 |

已验证：AAB 内部确实写入了隧道网址，没有残留 `10.0.2.2`。

---

## 六、日常怎么用

### 6.1 每次开机后要做的三件事

**第一步：启动后端**

```powershell
D:\apps\rentzu\run-server.cmd
```

**第二步：启动隧道（新开一个窗口，不要关掉它）**

```powershell
& "C:\Program Files (x86)\cloudflared\cloudflared.exe" tunnel --url http://127.0.0.1:8000
```

**第三步：从输出里找到网址**

会看到类似这样的方框：

```
+---------------------------------------------------------+
|  Your quick Tunnel has been created! Visit it at         |
|  https://xxxx-xxxx-xxxx-xxxx.trycloudflare.com           |
+---------------------------------------------------------+
```

### 6.2 如果网址变了（重启后必然会变）

因为网址被编译进了安装包，所以必须走完整流程：

```powershell
# 1) 把新网址写进 eas.json 的 production.env
#    D:\apps\rentzu\app\eas.json
#    "EXPO_PUBLIC_API_BASE_URL": "https://新的网址.trycloudflare.com"

# 2) 重新打包
cd D:\apps\rentzu\app
npx eas-cli@latest build -p android --profile production

# 3) 重新上传到 Google Play Console
```

**这就是为什么建议买域名** —— 否则每次重启电脑都要重复这一整套流程，
而 Google Play 的审核还要等一到几天。

### 6.3 验证隧道是否正常

```powershell
# 把网址换成你当前的
Invoke-WebRequest -Uri "https://你的网址.trycloudflare.com/docs" -UseBasicParsing
```

返回 `HTTP 200` 就说明通了。

---

## 七、将来怎么迁移到云服务器？

好消息：**我们已经把架构设计成"改一个网址就能切换"了。**

### 7.1 关键设计

`app/src/services/api.ts` 里现在是这样写的：

```ts
export const API_BASE_URL =
  process.env.EXPO_PUBLIC_API_BASE_URL ?? 'http://10.0.2.2:8000';
```

所有网络请求（`api.ts`、`voice.ts`、`reports.ts`）都走这一个变量。
所以**不管后端搬到哪里，只需要改这一个值再重新打包**，代码逻辑一行都不用动。

### 7.2 迁移路线图

```
现在：
   手机 ──► Cloudflare 快速隧道 ──► 你的电脑（FastAPI + PostgreSQL + Whisper）
                                    ⚠️ 电脑必须开机，网址会变

第一步（买域名，约 70 元/年）：
   手机 ──► Cloudflare 命名隧道 ──► 你的电脑（FastAPI + PostgreSQL + Whisper）
                                    ✅ 网址固定，仍然免费，仍然全部本地
                                    ⚠️ 电脑仍需开机

第二步（正式上线时）：
   手机 ──► Railway / Render ──► 云端 FastAPI ──► 云端 PostgreSQL（Neon 等）
                                    ✅ 电脑可以关机，24 小时可用
                                    ⚠️ 要迁移数据库，Whisper 要占内存（要钱）
```

**建议按这个顺序走**，不要一步到位。每一步都能独立验证，出问题好排查。

### 7.3 数据库怎么迁移？

你的 PostgreSQL 现在在本机。迁移到云端的步骤：

```powershell
# 1) 导出本地数据库（结构 + 数据）
pg_dump -U postgres -d rentzu -F c -f D:\apps\rentzu\rentzu_backup.dump

# 2) 在云服务商（推荐 Neon，有真正永久免费额度）创建一个 PostgreSQL 实例
#    会拿到一个连接串，形如：
#    postgresql://user:password@ep-xxx.neon.tech/rentzu

# 3) 恢复到云端
pg_restore -d "postgresql://user:password@ep-xxx.neon.tech/rentzu" D:\apps\rentzu\rentzu_backup.dump

# 4) 改后端的 DATABASE_URL 环境变量指向云端
```

数据库的连接地址读的是环境变量，所以代码同样不用改：

```python
# server/src/db.py
DATABASE_URL = os.getenv('DATABASE_URL', 'postgresql+psycopg://...localhost:5432/rentzu')
```

> 建议：迁移前先在云端跑通，确认没问题再切换，本地数据先别删。

### 7.4 迁移到云端时，Whisper 是个难点

这一点要提前知道：

你现在用的 `faster-whisper` 是**本地模型**，运行时要占用内存：

```
faster-whisper-tiny    74.6 MB 模型文件
faster-whisper-small  194.4 MB 模型文件
运行转录时大约需要 300~500 MB 内存
```

而免费云服务器通常只有 **512 MB 内存**，而且闲置一段时间会自动休眠
（下次请求要重新加载模型，很慢）。

所以到了第二步，你有两个选择：

| 方案 | 费用 | 隐私 | 说明 |
|---|---|---|---|
| **A. 云服务器加内存** | 约 5~7 美元/月 | 录音传到 Railway/Render | 代码不用改 |
| **B. 改用 OpenAI 语音 API** | 约 $0.006/分钟 | 录音传到 OpenAI | 要改代码，但免费套餐就够跑 |

你之前表示**不希望录音传给 OpenAI**。要注意：
**方案 A 其实录音也离开了你的电脑**（只是传给 Railway 而不是 OpenAI）。

如果隐私是首要考虑，那么**长期保持「命名隧道 + 本机运行」才是唯一真正私密的方案**。

---

## 八、常见问题

**Q：隧道会让我的电脑变慢吗？**
A：几乎不会。`cloudflared` 本身很轻量。真正吃资源的是 Whisper 转录，那个本来就在跑。

**Q：别人能通过这个网址攻击我的电脑吗？**
A：他们只能访问到你 FastAPI 暴露的那些接口，碰不到电脑上其他东西。
不过接口本身的安全性很重要 —— 我们已经修复了几个严重漏洞：
- 删除了 `/auth/dev-session` 免密登录后门
- 删除了固定验证码 `123456`
- 删除了 API 返回明文验证码的问题
- 修复了「输错密码会覆盖原密码」导致的账号被夺取漏洞
- 增加了验证码错误 5 次锁定

**Q：网址是随机英文单词，别人能猜到吗？**
A：基本猜不到，但这属于"靠隐蔽性保护"，不是真正的安全措施。
所以上面那些接口层面的加固才是关键。

**Q：我关掉那个黑色命令行窗口会怎样？**
A：隧道立刻断开，网址永久失效，所有测试员的 App 立刻连不上。
**在测试期间千万不要关掉它。**

**Q：测试员在国外能用吗？**
A：可以。Cloudflare 在全球都有节点，会自动就近接入。

---

## 九、下一步建议

按优先级排序：

1. **先自己装 AAB 到手机上测一遍**，确认整个流程真的通（这一步免费、立刻能做）
2. **买一个域名**（约 70 元/年），改用命名隧道，让网址固定下来
3. **准备隐私政策网页**（Google Play 强制要求）
4. **把 RevenueCat 正式密钥配置到 EAS**（现在打的包里没有，所以内购功能不可用）
5. 等真正要正式上线时，再考虑迁移到 Railway / Render + Neon

---

## 附录：本次改动涉及的文件

| 文件 | 改动 |
|---|---|
| `app/src/services/api.ts` | `API_BASE_URL` 改为读取 `EXPO_PUBLIC_API_BASE_URL` |
| `app/eas.json` | production 配置里加入 `env`，打包时注入网址 |
| `app/app.json` | 加入 App 图标、Android 自适应图标、启动图 |
| `server/src/services/auth.py` | 随机验证码、错误次数锁定 |
| `server/src/services/passwords.py` | 新增 `verify_password()`（之前根本没有验证密码） |
| `server/src/services/user_bootstrap.py` | 登录与注册拆分，不再覆盖密码 |
| `server/src/routers/auth.py` | 删除 `/auth/dev-session`，不再返回 `debug_code` |
| `server/src/services/email_delivery.py` | 输出目录改为可配置（原来写死 Windows 路径） |
| `schema_updates/2026-07-31-verification-code-attempts.sql` | 新增 `attempts` 字段 |
| `.agents/skills/rentzu-manual-testing/` | 自动测试改为从数据库读取真实验证码 |
